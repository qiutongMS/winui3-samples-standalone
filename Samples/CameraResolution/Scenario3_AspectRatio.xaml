<Page
    x:Class="SDKTemplate.Scenario3_AspectRatio"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid x:Name="RootGrid" Margin="12,20,12,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Text="Description:" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <TextBlock Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap">
          Match the aspect ratio of the video stream with the preview stream
                </TextBlock>
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Spacing="8">
                <ComboBox x:Name="PreviewSettings" PlaceholderText="Preview Settings" SelectionChanged="PreviewSettings_Changed" Width="Auto"
                          AutomationProperties.AutomationId="PreviewSettingsComboBox3"/>
                <ComboBox x:Name="VideoSettings" PlaceholderText="Video Settings" SelectionChanged="VideoSettings_Changed" Width="Auto"
                          AutomationProperties.AutomationId="VideoSettingsComboBox"/>
                <Button x:Name="VideoButton" Click="VideoButton_Click" IsEnabled="False"
                        AutomationProperties.AutomationId="VideoButton">Record Video</Button>
            </StackPanel>

            <Button Grid.Row="2" Content="Initialize Camera" Click="InitializeCameraButton_Click"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    AutomationProperties.AutomationId="InitializeCameraButton3"/>

            <MediaPlayerElement x:Name="PreviewControl" Grid.Row="2" Stretch="Uniform" Visibility="Collapsed"
                                AutoPlay="True" AutomationProperties.AutomationId="PreviewControl3"/>
        </Grid>
    </Grid>
</Page>
