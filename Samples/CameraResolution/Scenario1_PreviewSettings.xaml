<Page
    x:Class="SDKTemplate.Scenario1_PreviewSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid x:Name="RootGrid" Margin="12,20,12,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Text="Description:" Style="{StaticResource SubtitleTextBlockStyle}"/>
                <TextBlock Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap">
          Get and set all possible combinations of resolution and framerate settings for a given camera device.
                </TextBlock>
            </StackPanel>

            <ComboBox x:Name="CameraSettings" PlaceholderText="Preview Settings" SelectionChanged="ComboBoxSettings_Changed"
                        Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top" Width="Auto"
                        AutomationProperties.AutomationId="CameraSettingsComboBox"/>

            <Button Grid.Row="2" Content="Initialize Camera" Click="InitializeCameraButton_Click" HorizontalAlignment="Center" VerticalAlignment="Center"
                    AutomationProperties.AutomationId="InitializeCameraButton"/>

            <MediaPlayerElement x:Name="PreviewControl" Grid.Row="2" Stretch="Uniform" Visibility="Collapsed"
                                AutoPlay="True" AutomationProperties.AutomationId="PreviewControl"/>
        </Grid>
    </Grid>
</Page>
